# ECMC Parameter Configuration File Example
# Usage: ecmc_main -p params.txt
# Note: Command line options override settings in this file

# Required parameters
input_file = config_initial.dat

# Output settings
output_file = config_final.dat
binary_output = false

# ECMC parameters
n_chains = 1000000
chain_length = 1.0

# Sampling intervals
sample_interval_pressure = 50000     # Sample pressure every 50,000 chains
sample_interval_snapshot = 100000    # Save snapshot every 100,000 chains (0=disabled)

# Equilibration settings
n_equilibration_pressure = 100000    # First 100,000 chains used for equilibration

# Random number seed (0=random)
rng_seed = 42
